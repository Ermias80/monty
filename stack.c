#include "monty.h"

/**
 * stack - prints the top
 * @hd: stack head
 * @counter: line_number
 * Return: no return
 */
void stack(stack_t **hd, unsigned int counter)
{
	(void)hd;
	(void)counter;
	bus.lifi = 0;
}
